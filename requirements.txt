# Runtime dependencies.
#
# Each dependency is added at the phase in which it becomes necessary, so
# that this file documents the runtime surface and its origin.

# Phase 1.2 - Django application framework
Django>=5.2,<5.3
python-dotenv>=1.0,<2.0

# Phase 1.3 - Django REST Framework
djangorestframework>=3.16,<4.0

# Phase 1.4 - Official Elasticsearch Python client.
# Pinned to the 8.x series to match the Elasticsearch server version
# configured for local development in Docker Compose (Phase 1.6).
elasticsearch>=8.15,<9.0
